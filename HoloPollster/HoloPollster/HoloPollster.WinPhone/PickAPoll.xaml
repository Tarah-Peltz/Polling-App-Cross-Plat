<Page
    x:Class="HoloPollster.WinPhone.PickAPoll"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HoloPollster.WinPhone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <ScrollViewer>
        <Grid Name="grid">
            <Grid.RowDefinitions>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <StackPanel>
                <TextBlock x:Name="textBlock" HorizontalAlignment="Center" TextWrapping="WrapWholeWords" Text="which poll would you like to take?" VerticalAlignment="Center" FontSize="30" Margin="15,0"/>
                <Button x:Name="Cancel"  HorizontalAlignment="Left" VerticalAlignment="Bottom" Content="Cancel" Background="#FF00ABA9" Foreground="White" Click="Cancel_Click" Margin="12,0,0,0" BorderThickness="1" Height="96"/>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>
